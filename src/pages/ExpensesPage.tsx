import React from "react";

export const ExpensesPage = () => {
  // should fetch expenses and update state if they exist
  // should display a message if no expenses exist
  // should dipslay an add category form if no category exists
  return <div>ExpensesPage</div>;
};
